# TODO: Write documentation for `Iota`
module Iota
  module BundleValidator
    private def self.validate_bundle_signatures(bundle : String)
    end

    def self.is_bundle(bundle : String)
    end
  end
end
